                </main>
            </div>
        </div>
        <div class="modal fade" role="dialog" id="signin">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">Sign in with Discord</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <p>If you're participating in an event, choose "Just sign in" or add the bot to your server.</p>
                    </div>
                    <div class="modal-footer">
                        <a class="btn btn-primary" href="<%= server.config.addBot %>">Add to my server</a>
                        <a class="btn btn-secondary" href="/auth/discord">Just sign in</a>
                    </div>
                </div>
            </div>
        </div>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script src="https://mdbootstrap.github.io/bootstrap-material-design/assets/js/vendor/popper.min.js"></script>
        <script src="https://mdbootstrap.github.io/bootstrap-material-design/dist/js/bootstrap-material-design.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.28.0/feather.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.1.1/chart.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/snackbarjs/1.1.0/snackbar.min.js"></script>
        <script src="/dashboard.js"></script>
        <script>
        <% if(req.user && req.user.remainingSteps) { %>
        var setup = new Chart($('#setup')[0], {
            type: 'doughnut',
            data: {
                labels: [
                    ' Completed',
                    ' Steps left'
                ],
                datasets: [{
                    data: [<%= 2 - req.user.remainingSteps %>, <%= req.user.remainingSteps %>],
                    backgroundColor: ['#03a9f4', '#cacaca']
                }]
            },
            options: {
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    title: {
                        display: false,
                        text: 'Progress'
                    }
                }
            }
        });
        <% } %>
        </script>
    </body>
</html>